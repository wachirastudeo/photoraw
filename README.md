# Ninlab - คู่มือการใช้งานและการติดตั้ง

## 📖 สารบัญ
- [สำหรับผู้ใช้งาน](#สำหรับผู้ใช้งาน)
- [สำหรับนักพัฒนา](#สำหรับนักพัฒนา)
- [การสร้างตัวติดตั้ง](#การสร้างตัวติดตั้ง)

---

## สำหรับผู้ใช้งาน

### การติดตั้ง Ninlab

1. **ดาวน์โหลดไฟล์** `Ninlab.zip`
2. **แตกไฟล์** zip โดยดับเบิลคลิก
3. **ลาก** `Ninlab.app` ไปวางในโฟลเดอร์ **Applications**
4. **เปิดใช้งาน** จาก Launchpad หรือ Applications

### การเปิดครั้งแรก (สำคัญ!)

เนื่องจากแอปไม่ได้ลงนามด้วย Apple Developer Certificate ครั้งแรกที่เปิดจะมี warning:

**วิธีแก้:**
1. **คลิกขวา** ที่ `Ninlab.app` → เลือก **"Open"**
2. คลิก **"Open"** อีกครั้งในหน้าต่าง warning
3. หรือไปที่ **System Settings → Privacy & Security** → คลิก **"Open Anyway"**

หลังจากนั้นเปิดปกติได้เลย ไม่ต้องทำซ้ำอีก!

---

## สำหรับนักพัฒนา

### ความต้องการของระบบ

- macOS 10.13 หรือใหม่กว่า
- Python 3.9 หรือใหม่กว่า
- Git (สำหรับ clone repository)

### การติดตั้งสำหรับพัฒนา

```bash
# Clone repository
git clone <repository-url>
cd photoraw

# รันแอป (จะติดตั้ง dependencies อัตโนมัติ)
./run_mac.sh
```

สคริปต์ `run_mac.sh` จะ:
- สร้าง virtual environment (ถ้ายังไม่มี)
- ติดตั้ง dependencies ทั้งหมด
- รันแอป

### Dependencies

- **PySide6** - Qt framework สำหรับ GUI
- **numpy** - การประมวลผลข้อมูล
- **Pillow** - การประมวลผลภาพ
- **rawpy** - การอ่านไฟล์ RAW
- **scipy** - สำหรับ curve interpolation

---

## การสร้างตัวติดตั้ง

### วิธีที่ 1: ใช้สคริปต์อัตโนมัติ (แนะนำ)

```bash
./build_installer.sh
```

สคริปต์นี้จะ:
1. ติดตั้ง PyInstaller (ถ้ายังไม่มี)
2. Build `Ninlab.app` พร้อม dependencies ทั้งหมด
3. สร้างไฟล์ `Ninlab.zip` สำหรับแจกจ่าย

### วิธีที่ 2: Build ด้วยตนเอง

```bash
# เปิด virtual environment
source .venv/bin/activate

# ติดตั้ง PyInstaller
pip install pyinstaller

# Build แอป
pyinstaller Ninlab.spec

# สร้าง zip
cd dist
zip -r ../Ninlab.zip Ninlab.app
```

### ผลลัพธ์

หลัง build เสร็จจะได้:
- **`dist/Ninlab.app`** - แอปที่พร้อมใช้งาน
- **`Ninlab.zip`** - ไฟล์สำหรับแจกจ่าย (ประมาณ 50-100 MB)

### การแจกจ่าย

ส่งไฟล์ `Ninlab.zip` ให้ผู้ใช้ พวกเขาไม่ต้องติดตั้ง Python หรือ dependencies ใดๆ เพิ่มเติม

---

## โครงสร้างโปรเจค

```
photoraw/
├── main.py              # ไฟล์หลักของแอป
├── imaging.py           # ฟังก์ชันประมวลผลภาพ
├── workers.py           # Background workers
├── ui_helpers.py        # UI components และ helpers
├── catalog.py           # การจัดการ project catalog
├── export_dialog.py     # Dialog สำหรับ export
├── cropper.py           # Crop tool
├── curve_widget.py      # Tone curve widget
├── icon.ico             # ไอคอนแอป
├── run_mac.sh           # สคริปต์รันแอป (สำหรับพัฒนา)
├── build_installer.sh   # สคริปต์สร้างตัวติดตั้ง
├── Ninlab.spec          # PyInstaller configuration
└── README.md            # ไฟล์นี้
```

---

## การแก้ปัญหา

### แอปเปิดไม่ได้
- ตรวจสอบว่าได้ทำตามขั้นตอน "การเปิดครั้งแรก" แล้ว
- ลองคลิกขวา → Open แทนการดับเบิลคลิก

### Build ไม่สำเร็จ
```bash
# ลบไฟล์ build เก่า
rm -rf build dist

# ติดตั้ง dependencies ใหม่
source .venv/bin/activate
pip install --upgrade pip
pip install --upgrade pyinstaller PySide6 numpy pillow rawpy scipy

# Build ใหม่
./build_installer.sh
```

### แอปช้า
- ตรวจสอบว่าไม่ได้เปิดแอปอื่นที่ใช้ CPU/RAM มากเกินไป
- ลดขนาด Preview Size ในแอป

---

## ข้อมูลเพิ่มเติม

- **เวอร์ชัน:** 1.0.0
- **ลิขสิทธิ์:** Copyright © 2025 Wachira Studio
- **ใบอนุญาต:** [ระบุใบอนุญาตที่ใช้]

---

## การสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
- เปิด Issue ใน GitHub repository
- ติดต่อทีมพัฒนา
